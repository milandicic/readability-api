# Readability API - Environment Configuration
# When deployed in Coolify, these variables will be available to configure

# API Configuration
# The secret token for authenticating API requests (Required)
API_TOKEN=your-secret-token-here

# The port on which the API server will run
PORT=3000

# Rate Limiting Configuration
# Time window for rate limiting in milliseconds (15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum number of requests allowed per rate limit window
RATE_LIMIT_MAX=100

# HTTP Request Configuration
# Timeout for external URL requests in milliseconds (10 seconds)
AXIOS_TIMEOUT=10000 